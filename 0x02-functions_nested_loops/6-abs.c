#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * _abs - function to compute the absolute value of int
 * @n: checking value
 * Return: the value of n
 */
int _abs(int n)
{
	n = abs(n);
	return (n);
}

